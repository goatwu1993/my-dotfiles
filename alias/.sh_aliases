#!/bin/bash

dbash() { docker exec -it $@ bash ;}
dlog()  { docker logs --tail=all -f $@ ;}
kbash() { kubectl exec -it $@ -- bash ;}

alias fy="fuck -y"
alias gs="git status"
alias ll="ls -lAh"
alias tree="tree -a"
