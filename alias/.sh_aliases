#!/bin/bash

#alias dockerip="docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'"
#alias dockerrs="docker restart $(docker ps -aq)"
#alias dockerrm="docker stop $(docker ps -aq); docker remove $(docker ps -aq)"
alias dpsf="docker ps --format '{{.Names}}\n\tContainer ID: {{.ID}}\n\tCommand: {{.Command}}\n\tImage: {{.Image}}\n\tCreatedAt: {{.CreatedAt}}\n\tStatus: {{.Status}}'"


# brew
alias b="brew"

# clear
alias c="clear"

# docker
alias dexec="docker exec"
dbash() { docker exec -it $@ /bin/bash; }
alias dpsa="docker ps -a"

# docker-compose
alias d-c="docker-compose"

# git
alias gs="git status"

# grep
alias grepri="grep -ri"

# nvm
alias nvm_load='export NVM_DIR="$HOME/.nvm" && \
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && \
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"'
#alias node='nvm_load && node'
#lias npm='nvm_load && npm'

# psql
alias pg_start="launchctl load ~/Library/LaunchAgents/homebrew.mxcl.postgresql.plist"
alias pg_stop="launchctl unload ~/Library/LaunchAgents/homebrew.mxcl.postgresql.plist"

# python
alias py="python"

# tree
alias tree2="tree -L 2"
alias tree3="tree -L 3"


# vim
alias v="nvim"
alias vi="nvim"
alias nv="nvim"
alias nvi="nvim"
alias vim="nvim"
alias vimrc="nvim ~/.config/nvim/init.vim"
alias nvimrc="nvim ~/.config/nvim/init.vim"

# ssh
alias sshconfig="cat ~/.ssh/config"

# zsh
alias zshrc="nvim ~/.zshrc"


# Work
# alias ssh-production-ws="ssh linker@52.183.19.93"
# alias ssh-production-druid="ssh linker@13.77.182.49"
# alias ssh-stage-ws="ssh linker@52.183.62.6"
# alias ssh-stage-druid="ssh linker@52.151.50.223"
